```mermaid
sequenceDiagram
    actor browser
    actor server
    Note right of browser: Clicking Save button trigger the form onsubmit callback
    Note right of browser: New note is created and added to the "notes" array
    Note right of browser: DOM is redrawn to show the new note
    
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note right of browser: 201 created with body contain content + date as JSON
```